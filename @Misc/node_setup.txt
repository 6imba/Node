# Install node via terminal:
	$ curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash - &&\
	$ sudo apt-get install -y nodejs

# Check version:
	$ node -v
	$ npm -v
	$ npx -v
	
# Vue setup via vue/cli:
	$ sudo npm install apk -g @vue/cli
	$ vue --version
	
# 	
	
